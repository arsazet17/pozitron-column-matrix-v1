name: Legacy KENO history updater (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled safely
        run: echo "Legacy copy workflow is disabled. Recovery workflow remains active."
